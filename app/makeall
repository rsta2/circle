#!/bin/bash

make="${MAKE:-make}"

cd lib

cd zxsmi
$make $1 $2 || exit
cd ..

# TODO: Add new libs here
#cd subdir
#$make $1 $2 || exit
#cd ..

cd ..

cd hdspeccys
$make $1 $2 || exit
cd ..

# TODO: Add new apps here
#cd subdir
#$make $1 $2 || exit
#cd ..
